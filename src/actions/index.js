import bpi from './bpi';
import stopLoading from './stopLoading';
import startLoading from './startLoading';

const actions = {
    bpi,
    stopLoading,
    startLoading,
}

export default actions;
export {
    bpi,
    stopLoading,
    startLoading,
};